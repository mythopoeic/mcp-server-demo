"""sheet-compressor MCP server — a pure consumer of the upstream library.

Tool functions are exported from :mod:`sheet_compressor_mcp.tools` and registered
with FastMCP in :mod:`server`. Tests target the tool functions directly (Seam 1).
"""
